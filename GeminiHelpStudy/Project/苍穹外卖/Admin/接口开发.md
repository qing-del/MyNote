## 新增员工接口
- `BeanUtils` - `Spring`中提供的工具包
```Java
// 利用 Spring 工具包中拷贝属性  
BeanUtils.copyProperties(employeeDTO, employee);
```
> [!tip] ⚠ 注意
> 这个方法仅会拷贝**同变量名**的属性

---

## 员工分页查询
- `PageHelper`
- `Web Mvc`配置文件中的 消息转换器`converters`
```java
// WebMvcConfiguration.java
protected void extendMessageConverters(List<HttpMessageConverter<?>> converters) {  
    log.info("Extended message converter...");  
    // 1. 创建一个转换器类  
    MappingJackson2CborHttpMessageConverter converter = new MappingJackson2CborHttpMessageConverter();  
    // 2. 给转换器设置映射器  
    converter.setObjectMapper(objectMapper);  
    // 3. 将转换器加入到转换器集合中  
    converters.add(0, converter);  
}
```
> [!warning] 调用`add`方法中第一个行参是可以设置转换器的优先级的，如果默认放到最后会很可能用不上

- `interceptor` 拦截器的话，不能用`try-catch-finally`来清除`ThreadLocal`
```java
@Override  
public void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex) {  
    BaseContext.removeCurrentId();  
    log.info("Clean up thread-local data");  
}
```

---

## 员工修改密码

---

## 分类管理接口开发

---

## 菜品分页查询
- SQL查询语句的外连接、内连接的运用已忘记
```sql
select d.*, c.name as category_name from dish d left outer join category c on d.category_id = c.id;
```

---

## 菜品接口开发

---

## 套餐接口开发

---

## 店铺营业状态开发
- 如果项目中存在两个类名相同的`bean`，会报错
	- 例如：
		- `controller.admin.ShopController`
		- `controller.user.ShopController`
- 可以通过`@RestController("name")`指定名字来解决

---

## 套餐接口
> [!info] 个人日志
> 开发`enable or disable setmeal`接口的时候
> `interceptor`层一直检测方法是`HandlerMethod` 导致`Controller`一直收不到
> 我在前端消息弹窗中看见`404`，就知道是没连到接口，以为是请求类型错了，将`get`->`put`->`post`
> 然后我再三确定确实是`post`之后，我再认真一看，发现结果是写成`/{status}`了，正确是`/status/{status}`

---

## Redis 缓存降低数据库压力
> [!tip] 💡小技巧
> 如果存储的实体类**支持序列化**的话
> 可以将数据保存为`Redis`中的`String`类型，这个过程底层依赖会自动帮你进行序列化和反序列化

> [!warn] ⚠ 注意
> 用`Redis`存储数据的时候
> 如果数据发生变动，需要清理`Redis`中缓存的数据
> 及时对数据进行更新操作

- 引入`Spring Cache`依赖

---

## 订单状态自动处理
- 可以使用`Spring Task`来实现定时处理
- 学习`Spring Task` -> [[Spring Task]]
- 