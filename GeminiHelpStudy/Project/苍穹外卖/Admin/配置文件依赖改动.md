### 🚨 步骤 1：铲除“毒瘤”依赖 (Cleanup)
你的 `sky-pojo` 模块里有一个致命错误，这在企业级开发中是会被安全合规部门打回的。

**问题定位 (`pom (2).xml`)：**
```XML
<dependency>
    <groupId>com.fasterxml.jackson.core</groupId>
    <artifactId>jackson-databind</artifactId>
    <version>2.9.2</version> 
</dependency>
```
- **为什么这是错的？** Jackson 2.9.2 是 2017 年的版本，存在高危反序列化漏洞。而且 Spring Boot 3 本身内置了 Jackson 2.15+，你这里手动强制降级，会导致 Spring 内部功能报错。
- **修正操作：** **直接删除这段 dependency**。让它跟随父工程的 Spring Boot 管理版本。


**问题定位 (`pom.xml` 父工程)：**
- **Fastjson 1.2.76**: ❌ 拒绝使用。Spring Boot 默认使用 Jackson。在同一个项目里混用两个 JSON 库是工程大忌（序列化行为不一致）。
    - _修正_：删除 Fastjson 依赖，统一使用 Jackson。
- **Commons-Lang 2.6**: ❌ 这是十年前的老古董。
    - _修正_：升级为 `org.apache.commons:commons-lang3`。
    - 
---

### 🛠️ 步骤 2：升级 Spring Boot 与引入 Spring AI (Upgrade)

为了让你的项目支持 **AI Agent** 和 **RAG**，我们需要升级 Spring Boot 版本以匹配 Spring AI 的最佳实践。

**请修改父工程 `pom.xml`：**
1. **升级 Spring Boot 版本** (为了更好的 AI 支持):
    ```XML
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version> <relativePath />
    </parent>
    ```
    
2. **引入 Spring AI 的 BOM (Bill of Materials)**: 在 `<dependencyManagement>` 标签内部，**最前面**加入：
    ```XML
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>0.8.1</version> <type>pom</type>
        <scope>import</scope>
    </dependency>
    ```
    
3. **配置阿里云 Maven 仓库无法下载 Spring AI 的问题**: Spring AI 主要是 Milestone 版本，国内镜像源可能不同步。你需要确保 `pom.xml` 底部有以下仓库配置（如果没有请添加）：
    ```XML
	<project>
	    <repositories>
	        <repository>
	            <id>spring-milestones</id>
	            <name>Spring Milestones</name>
	            <url>https://repo.spring.io/milestone</url>
	            <snapshots>
	                <enabled>false</enabled>
	            </snapshots>
	        </repository>
	        <repository>
	            <id>spring-snapshots</id>
	            <name>Spring Snapshots</name>
	            <url>https://repo.spring.io/snapshot</url>
	            <releases>
	                <enabled>false</enabled>
	            </releases>
	        </repository>
	    </repositories>
	</project>
    ```

> [!warning] ❗注意
> 记得修改 Maven 的`settings.xml`，可以去其安装目录进行寻找
> ```xml
> <mirror>
>    <id>aliyunmaven</id>
>    <!-- 新加入的<mirrorOF>，防止被请求被拦截 -->
>    <mirrorOf>*,!spring-milestones,!spring-snapshots</mirrorOf>  
>    <name>阿里云公共仓库</name>
>    <url>https://maven.aliyun.com/repository/public</url>
> </mirror>
> ```

---

### 📦 步骤 3：规范化 Lombok 和 Knife4j

你的 `knife4j` 版本是 3.0.3，这主要设计给 Swagger 2。Spring Boot 3 已经全面拥抱 OpenAPI 3 规范。

**修改父工程 `pom.xml` 的 `<properties>`：**
```XML
<properties>
    <java.version>17</java.version>
    <knife4j>4.3.0</knife4j> 
    <lombok>1.18.30</lombok> 
    </properties>
```

**修改父工程 `dependencyManagement` 中的 Knife4j：**
```XML
<dependency>
    <groupId>com.github.xiaoymin</groupId>
    <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId> <version>${knife4j}</version>
</dependency>
```
_(注意：子模块引用 Knife4j 时也要同步修改 artifactId)_

---

### 🚀 你的 Action Item

1. **执行上述修改**：重点是删除 `jackson-databind` 的版本锁定，升级 Parent Version，并引入 Spring AI BOM。
2. **Reload Maven**：在 IDE 中刷新 Maven，确保没有红色波浪线。
3. **验证启动**：尝试运行 `SkyApplication`。
    - _预期可能报错_：因为升级了 Knife4j 和删除了 Fastjson，有些原来的代码（比如 `JSONObject` 或 Swagger 注解）可能会报错。
    - **这是好事**。这逼着你现在就去把那些不规范的代码修好，而不是留到后面炸雷。