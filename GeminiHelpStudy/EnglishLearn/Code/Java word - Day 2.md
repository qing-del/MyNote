## Words and machine-translated sentences
- `enable` - 启用
	- `enable or diable setmeal`
	- 原句：启用或者禁用套餐
- `oversight` - 遗漏、忽略
- `layer` - 层
- `regard` - 与……相关
	- `Fix the oversight in the Mapper layer regarding `AutoFill``
	- 原句：修复Mapper层关于`AutoFill`的遗漏问题
- `Batch` - 批量、捆
	- `Batch insertion of menu items`
	- 原句：批量插入套餐菜品
- `comment` - 评论
	- `Add some comments for the interface descriptions that have already been developed.`
	- 原句：新增一些已经开发好的接口说明注释
- `due to` - 由于
- `issue` - 问题、风险
- `subsequent` - 随后的
- `access` - 访问、获取
	- `Due to the issue of deleting the image first, if an error occurs during the subsequent deletion of the setmeal when the transaction method is enabled, the photo will be deleted, but the setmeal will not be deleted. As a result, when accessing the photo URL later, the image cannot be returned normally.`
	- 原句：由于先删除image的问题，如果在开启事务方法，在后续删除套餐中出现报错，会导致照片被删除了，但是套餐没被删除，后续访问照片URL的时候无法正常返回图片
- `dynamic` - 动态的
	- `Dynamic conditions query setmeal.`
	- 原句：动态条件查询套餐
- `product` - 商品、产品
	- `Add the product to the shopping cart.`
	- 原句：添加商品到购物车
- `quantity` - 数量
	- `if product is existed, increase the quantity`
	- 原句：如果商品存在，则增加商品的数量

---

## Self-translated sentence
1. context
	- Chinese: 根据分类id查询套餐的数量
	- English: Queried the number of setmeal based on category id.
2. context
	- Chinese: 完成`Xxx`接口的开发
	- English: Completed the development of the interface about `Xxx`.
3. context
	- Chinese: 先插入套餐
	- English: Prioritize inserting the setmeal. 
4. context
	- Chinese: 使用其生成并返回的`id`键值给`setmeal_dish`赋值
	- English: Using the generated and returned `id` key to assign `setmeal_dish`.
5. context
	- Chinese: 完成根据分类id查询菜品的接口开发
	- English: Completed the development of the interface regarding query dish based on category id.
6. context
	- Chinese: 完成新增套餐接口的开发
	- English: Completed the development of the interface regarding add new setmeal.
7. context
	- Chinese: 批量删除套餐
	- English: Delete batch setmeal based on id list.
8. context
	- Chinese: 给一些方法增加接口文档注释
	- English: Add annotations in interface document to some methods.
9. context
	- Chinese: 完成根据`ids`批量删除套餐接口的开发
	- English: Completed the development of interface regarding batch delete setmeal based on ids list.
10. context
	- Chinese: 先删除原来的套餐菜品(`setmeal_dish`)
	- English: Prioritize delete original `setmeal_dish`
11. context
	- Chinese: 将`setmealDTO`的属性拷贝到`setmeal`
	- English: Copied `setmealDTO` properties to `setmeal`
12. context
	- Chinese: 检查`setmeal`的`image`是否和之前一样
	- English: Check whether `setmeal's image` is same as the before
13. context
	- Chinese: 完成通过`id`获取`setmealVO`对象接的开发
	- English: Completed the development of the interface regarding to obtain `setmealVO` object based on id.
14. context
	- Chinese: 完成更新`Setmeal`的接口开发
	- English: Completed the development of the interface regarding to update `Setmeal`.
15. context
	- Chinese: 根据套餐id查询菜品选项
	- English: Querying dish options based on `setmeal` id
16. context
	- Chinese: 根据套餐id查询包含的菜品列表
	- English: Query dish list based on `setmealId`
17. context
	- Chinese: 根据套餐id查询包含的菜品列表
	- English: Query dishes included in `setmeal` based on `setmealId`.
18. context
	- Chinese: 此外，你需要加到公共方法上。因为你需要知道，同一个类内方法间互相调用会导致缓存注解失效。
	- English: More, you need to add public method. Because you need to know that methods internally call each other in same class will lead Cache Annotations fail to function.
19. context
	- Chinese: 
	- English: 
20. context
	- Chinese: 
	- English: 

---

## The word or phrase under investigation
1. `prioritize` -> 优先做...
2. `assign` -> 赋值、分配
3. `explanation note` -> 说明注释
4. `internally` -> 内部地