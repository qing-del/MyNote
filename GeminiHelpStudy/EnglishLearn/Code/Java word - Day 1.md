> [!tip] 💡Tip
> Try to write Java annotations in English

## Words and machine-translated sentences
- `annotations` - 注解
	- `Try to write Java annotations in English`
	- 原句：尝试用英语写Java注解
- `obtain` - 获取
- `interface` - 接口
	- `Obtain the openid by using WeChat login interface`
	- 原句：调用微信登录接口获取openid
- `call` - 调用
	- `Call interface`
	- 原句：调用函数
- `object` - 对象 -> `returned object` - 返回对象
- `check`
	- `Check if the openid is empty`
	- 原句：判断openid是否为空
- `query` - 查询
	- `Query whether there is user based on the openid`
	- 原句：根据openid查询是否有用户
- `automatically` - 自动地
	- `If it is a new user, the registration will be automatically completed.`
	- 原句：如果是新用户则自动完成注册
- `occur` - 出现
- `format` - 格式
	- `An error occurred while processing the JSON format.`
	- 原句：处理JSON格式时出现错误
- `since` - 由于
- `member variable` - 成员变量
- `verification` - 校验
	- `Interceptor for JWT token verification`
	- 原句：校验jwt令牌的过滤器
- `determine` - 查明、判断
- `whether` - 是否、两个中选一个
	- `Determine whether the currently intercepted method is for the Controller or for some other resource.`
	- 原句：判断当前拦截到的是Controller的方法还是其他资源
- `unblock` - 放行
- `otherwise` - 否则
	- `The following unblocking paths must be added; otherwise, the document cannot be loaded.`
	- 原句：必须添加以下放行路径，否则文档无法加载
- `Construct` - 构造
	- `Construct the return data`
	- 原句：构造返回对象


---

## Self-translated sentence
1. one
	- Chinese: 注册用户过滤器
	- English: Register user interceptor
2. two
	- Chinese: 由于本人写的`AliOssUtil`类 采用了读取环境变量的方式来获取值
	- English: Since the `AliOssUtil` class written by me adopts a method to read environment variable to obtain value
3. three
	- Chinese: 所以需要配置环境变量`OSS_ACCESS_KEY_ID`和`OSS_ACCESS_KEY_SECRET`
	- English: So the `OSS_ACCESS_KEY_ID` and `OSS_ACCESS_KEY_SECRET` environment variable need to be configured.
4. four
	- Chinese: 配置 阿里云OSS 的环境变量
	- English: Configure environment variable of the `AliyunOSS`
5. five
	- Chinese: 先设置环境变量
	- English: Set environment variable
6. six
	- Chinese: 应用被设置好的环境变量
	- English: Apply the preset environment variable 
7. seven
	- Chinese: 检测环境变量
	- English: Check the environment variable
8. eight
	- Chinese: 检查返回响应中是否包含`openid`
	- English: Check the response has `openid`
9. nine
	- Chinese: 
	- English: 

---

## The word or phrase under investigation
- 本人写的 -> `Written by me`
- 采用 -> `adopt`
- 配置 -> `configure`
- 预置 -> `preset`